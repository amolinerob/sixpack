// Shared SVG layout only; domains and data remain owned by each chart.
export const chartLayout = {
  width: 320,
  height: 200,
  inset: { top: 24, right: 12, bottom: 28, left: 44 },
  pointRadius: 2,
} as const
